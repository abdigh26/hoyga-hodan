/*
  Hoyga Hodan public site data
  Historical figures reconstruct the first four gatherings and are participation
  figures, not audited impact results. Update this file when source records are
  confirmed; counter markup reads its values from this object at runtime.
*/
window.HOYGA_HODAN_SITE_DATA = Object.freeze({
  historicalTrackRecord: Object.freeze({
    eventsDelivered: 4,
    womenParticipantsReached: 600,
    paidParticipants: 422,
    freeParticipants: 178
  }),
  plannedProgramme: Object.freeze({
    events: 9,
    cities: 4
  })
});
